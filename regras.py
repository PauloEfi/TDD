def calcular_desconto(valor):
    return valor * 0.90


def calcular_desconto_vip(valor):
    return valor * 0.80


def calcular_media(nota1, nota2, nota3):
    return (nota1 + nota2 + nota3) / 3


def verificar_aprovacao(media):
    return media >= 7